@model TrafficTicketMan.Models.IDType

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>IDType</legend>

        @Html.HiddenFor(model => model.IDTypeId)

        <div class="editor-label">
            @Html.LabelFor(model => model.IDDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.IDDescription)
            @Html.ValidationMessageFor(model => model.IDDescription)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
