@model IEnumerable<TrafficTicketMan.Models.Nationality>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Country)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Country)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NationalityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NationalityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NationalityId })
        </td>
    </tr>
}

</table>
