@model TrafficTicketMan.Models.OffenceType

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>OffenceType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.OffenceName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OffenceName)
            @Html.ValidationMessageFor(model => model.OffenceName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.OffenceDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.OffenceDescription)
            @Html.ValidationMessageFor(model => model.OffenceDescription)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
