@model IEnumerable<TrafficTicketMan.Models.OffenceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OffenceName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OffenceDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OffenceName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OffenceDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OffenceTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OffenceTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OffenceTypeId })
        </td>
    </tr>
}

</table>
