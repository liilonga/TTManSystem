@model IEnumerable<TrafficTicketMan.Models.PeaceOfficerCapacity>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Capacity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PeaceOfficerCapacityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PeaceOfficerCapacityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PeaceOfficerCapacityId })
        </td>
    </tr>
}

</table>
