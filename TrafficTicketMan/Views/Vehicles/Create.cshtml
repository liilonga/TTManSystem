@model TrafficTicketMan.Models.OffenderVehicle

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>OffenderVehicle</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.VehicelRegistrationNumber)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.VehicelRegistrationNumber)
            @Html.ValidationMessageFor(model => model.VehicelRegistrationNumber)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
