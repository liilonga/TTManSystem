@model IEnumerable<TrafficTicketMan.Models.OffenderVehicle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehicelRegistrationNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicelRegistrationNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OffenderVehicleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OffenderVehicleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OffenderVehicleId })
        </td>
    </tr>
}

</table>
